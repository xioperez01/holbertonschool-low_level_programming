#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/ * betty style doc para la función main va allí * /
int main (nulo)
{
  int n;

  srand (tiempo (0));
  n = rand () - RAND_MAX / 2;
  / * tu código va allí * /
      retorno (0);
}